DIAGRAM TRIGGERING RULES:
1. Call `generate_diagram` when introducing complex spatial, algorithmic, or structural concepts.
2. Ensure the `concept` parameter passed to the tool is highly specific and descriptive (it writes and executes Python matplotlib code, saving a .png to the Obsidian attachment folder and embedding it).
3. Do not generate diagrams for simple abstract definitions where text is clearer.